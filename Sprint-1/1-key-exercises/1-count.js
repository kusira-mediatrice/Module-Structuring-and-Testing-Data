let count = 0; // Line 1 is a variable declaration, creating the count variable with an initial value of 0

count = count + 1; //line 3 is an expression assigning the addiction operation of count and 1 into the variable count
//ie,count =0 + 1 so the = operetor is used to Assign(0 + 1) to the variable count
console.log(`New value of count=  ${count}`);

// Describe what line 3 is doing, in particular focus on what = is doing
